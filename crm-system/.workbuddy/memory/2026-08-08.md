# 2026-08-08 工作记录

## 数据模型重构完成

### 完成的改动
1. **删除 Sale 表**
   - 移除 schema.prisma 中的 Sale 模型和所有关联
   - 删除 routes/sales.ts 路由文件
   - 移除 index.ts 中的路由注册
   - 更新 dashboard.ts 移除销售统计
   - 更新 projectCosts.ts 移除销售统计
   - 移除前端所有 sale 相关代码（api.ts、routeConfig.ts、SaleList.tsx）

2. **ContractPayment 重命名为 ContractReceipt**
   - schema.prisma: ContractPayment → ContractReceipt
   - contractPayments.ts → contractReceipts.ts
   - 更新所有前端调用（ProjectDetail.tsx、api.ts）
   - 更新 index.ts 路由注册

3. **修复 OrgType.BRANCH 注释**
   - 注释从"个人"改为"分公司"

### Bug 修复
修复了3个测试发现的bug：

1. **归档保护失效**
   - 问题：已归档项目仍可编辑和删除
   - 修复：在 projects.ts 的 PUT 和 DELETE 路由中添加归档状态检查
   - 验证：已归档项目编辑返回403，删除返回403

2. **采购创建返回500**
   - 问题：缺少vendor字段时Prisma抛出异常
   - 修复：在 procurements.ts 添加必填字段校验，返回400
   - 验证：缺少vendor时返回400 "采购标题和供应商名称不能为空"

3. **出差创建返回500**
   - 问题：缺少必填字段时Prisma抛出异常
   - 修复：在 businessTrips.ts 添加必填字段校验，返回400
   - 验证：缺少必填字段时返回400 "出差标题、目的地、开始日期、结束日期和目的不能为空"

### 自动化测试
- 创建了全面的测试脚本 `test-full.js`
- 覆盖14个模块、60个测试用例
- 发现3个真实bug，已全部修复并验证通过

### Git 提交
```
63013e4 refactor: 删除Sale表，重命名ContractPayment为ContractReceipt

- 移除Sale模型及所有关联代码
- ContractPayment重命名为ContractReceipt
- 修复OrgType.BRANCH注释
```


### 待办事项
- 项目成本统计接口路由可能不是 `/project-costs/:id`，需要确认
- 其他模块的必填字段校验可能需要补充

### 18:35 UI 修复
- **联系人部门字段**：将客户管理中添加联系人弹窗的"部门"字段从 Select 下拉框改为 Input 输入框，移除了 11 个硬编码的部门选项，改为自由输入

## 项目归档页面修复

### 问题
1. 项目归档菜单点击后跳转到工作总览页面
2. 两个页面共用同一个组件，数据和样式完全一样

### 修复内容
1. **数据库菜单配置**
   - 将"项目归档"菜单的 path 从 `/sales` 改为 `/projects/archived`
   - 数据库 ID: 3, key: sales

2. **路由配置**
   - 在 routeComponents 中注册 `/projects/archived` 路由
   - 路由指向新创建的 ProjectArchive 组件

3. **菜单导航逻辑**
   - 修复 Layout.tsx 中菜单项 key 的生成逻辑
   - 现在优先使用 menu.path 字段，而不是 menu.key
   - 修复 selectedKeys 逻辑，支持多级路径

4. **独立的项目归档页面**
   - 创建 ProjectArchive.tsx 组件
   - 只查询已归档项目（isArchived: 'true'）
   - 隐藏新增、编辑、删除、导入等编辑按钮
   - 只显示查看和导出功能
   - 隐藏"归档状态"筛选器
   - 添加"归档时间"列显示归档日期

5. **后端排序字段**
   - 在 projects.ts 的排序字段白名单中添加 'archivedAt'
   - 归档页面按归档时间降序排列

### 验证结果
- 项目管理页面：显示未归档项目，支持完整 CRUD
- 项目归档页面：只显示已归档项目，只读模式，橙色主题
- 后端 API 支持 archivedAt 排序

