# 2026-07-31 工作记录

## 新用户菜单权限问题修复

**问题**：服务器部署后，新创建的用户登录看不到任何菜单

**根因**：创建用户时只设置了 `User.roleId`，但菜单加载逻辑优先查 `UserRole` 表，两个表数据不同步

**修复**：修改 `backend/src/routes/users.ts` 的创建用户逻辑，在创建用户的同时创建 `UserRole` 关联记录

**待部署**：需要重新打包后端镜像并部署到服务器
