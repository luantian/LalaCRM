# 2026-08-11 工作记录

## 完成的主要工作

### 1. 全面代码审查与问题修复

完成了对 LalaCRM 项目的全面代码审查，共发现并修复了 14 个问题：

#### 🔴 高严重度问题（3个）

1. **PrismaClient 单例化**
   - 问题：项目中存在 44 个 `new PrismaClient()` 实例，浪费连接资源
   - 修复：创建 `backend/src/lib/prisma.ts` 单例模块，所有文件统一导入使用
   - 影响：减少数据库连接数，提升性能

2. **WebSocket 心跳定时器泄漏**
   - 问题：WebSocket 心跳检测定时器在进程关闭时未清理
   - 修复：添加 `cleanupWebSocket()` 函数，在 `gracefulShutdown()` 中调用
   - 影响：避免内存泄漏和定时器堆积

3. **项目付款完结过滤优化**
   - 问题：`fullyPaid=true` 时先查询全部项目再内存过滤，数据量大时性能差
   - 修复：使用原生 SQL 在数据库层面完成过滤和分页
   - 影响：大数据量下性能显著提升

#### 🟡 中严重度问题（5个）

4. **合同列表数据权限**
   - 问题：合同列表缺少数据权限过滤，任何人都能看到所有合同
   - 修复：添加 `getContractDataScope()` 函数，非管理员只能看到自己创建的或项目团队成员的合同
   - 影响：数据安全

5. **管理员判断逻辑统一**
   - 问题：登录时使用 `user.role === 'ADMIN'`，权限检查使用 `UserRole` 表，判断不一致
   - 修复：统一使用 `isAdmin(userId)` 函数判断
   - 影响：避免权限判断混乱

6. **新角色默认数据范围**
   - 问题：创建角色时默认 `dataScope='ALL'`，过于宽松
   - 修复：改为默认 `SELF`
   - 影响：更安全的默认配置

7. **Blob 请求错误信息丢失**
   - 问题：`responseType: 'blob'` 时，错误响应也是 blob，无法直接读取错误信息
   - 修复：拦截器中特殊处理 blob 错误响应，尝试解析为 JSON
   - 影响：改善用户体验，能看到具体错误信息

8. **数据库备份同步 IO**
   - 问题：使用 `writeFileSync/readFileSync` 阻塞主线程
   - 修复：改为 `fs.promises` 异步读写
   - 影响：避免阻塞 Node.js 事件循环

#### 🟢 低严重度问题（6个）

9. **任务列表无分页**
   - 修复：添加分页支持，默认 50 条，最多 100 条

10. **Dashboard 加载过多项目**
    - 修复：将 `pageSize: 1000` 改为 100

11. **JWT 有效期过长**
    - 修复：从 7 天改为 24 小时

12. **自动日报错误静默**
    - 修复：所有 `.catch(() => {})` 改为 `.catch(err => logger.warn(...))`，记录错误日志

13. **downloadFile 无 token 过期处理**
    - 修复：检查响应状态，401/403 时自动跳转到登录页

14. **TypeScript 编译错误修复**
    - 修复 `auth.ts` 中 `isAdmin` 未导入
    - 修复 `projects.ts` 中 `parseInt` 参数类型问题

### 2. 验证结果

- ✅ TypeScript 编译通过（`tsc --noEmit`）
- ✅ 后端服务健康检查通过
- ✅ 所有 14 个问题已修复并验证

## 技术细节

### PrismaClient 单例化实现

创建 `backend/src/lib/prisma.ts`：
```typescript
import { PrismaClient } from '@prisma/client'

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined
}

export const prisma = globalForPrisma.prisma ?? new PrismaClient()

if (process.env.NODE_ENV !== 'production') {
  globalForPrisma.prisma = prisma
}
```

所有路由文件统一导入：
```typescript
import prisma from '../lib/prisma'
```

### WebSocket 清理实现

```typescript
export function cleanupWebSocket() {
  if (heartbeatTimer) {
    clearInterval(heartbeatTimer)
    heartbeatTimer = null
  }
  wss?.close()
}
```

在 `gracefulShutdown()` 中调用：
```typescript
cleanupWebSocket()
```

### 项目付款完结 SQL 优化

使用原生 SQL 在数据库层面完成过滤：
```sql
SELECT p.id FROM "Project" p
WHERE ...
AND EXISTS (
  SELECT 1 FROM "Contract" c 
  WHERE c."projectId" = p.id AND c."deletedAt" IS NULL
)
AND (
  SELECT COALESCE(SUM(CAST(c.amount AS DECIMAL)), 0)
  FROM "Contract" c 
  WHERE c."projectId" = p.id AND c."deletedAt" IS NULL
) <= (
  SELECT COALESCE(SUM(CAST(c.amount AS DECIMAL)), 0)
  FROM "Contract" c
  JOIN "ContractReceipt" cr ON cr."contractId" = c.id
  WHERE c."projectId" = p.id AND c."deletedAt" IS NULL AND cr."deletedAt" IS NULL
)
```

## 下一步建议

1. 运行完整的自动化测试套件验证功能
2. 考虑是否需要添加更多的数据权限控制
3. 监控修复后的性能表现
4. 继续处理任务列表中的其他待办事项

### 3. 数据权限逻辑统一改造（夜间）

将项目中存在的四种数据权限模式统一为增强版 `applyDataScope` 中间件：

**改造前的问题：**
- `applyDataScope` 中间件（7个模块正常使用）
- `getDataFilter` / `getUserDataScope` 死代码（从未被调用）
- Project / Contract 自定义内联逻辑（忽略角色 dataScope 配置）
- Procurement 完全无数据权限过滤
- Opportunity 同一文件内混用两种方式（列表用内联，导出用中间件）

**改造内容：**

1. **`middleware/dataScope.ts`** — 核心增强
   - `getDataScopeWhere` 和 `applyDataScope` 参数从 positional 改为 `ModelScopeConfig` 对象
   - 新增 `relations` 配置，支持 Contract/Procurement 通过 `project` 关联查 owner/teamMembers
   - 新增 `RelationScopeConfig` 和 `ModelScopeConfig` 类型导出

2. **`routes/projects.ts`** — 删除 `getProjectScopeWhere`，5个路由改用 `applyDataScope({ ownerField: 'ownerId', teamMemberField: 'teamMembers' })`

3. **`routes/contracts.ts`** — 删除内联逻辑，4个路由改用 `applyDataScope({ ownerField: 'ownerId', relations: [{ path: 'project', ownerField: 'ownerId', teamMemberField: 'teamMembers' }] })`

4. **`routes/procurements.ts`** — 从无权限到有权限，3个路由加 `applyDataScope({ ownerField: 'assignedTo', relations: [...] })`

5. **`routes/opportunities.ts`** — 删除内联逻辑，5个路由统一使用 `applyDataScope({ ownerField: 'ownerId', teamMemberField: 'teamMembers' })`

6. **`routes/dashboard.ts`** — 去掉中间件，改为路由内按模型分别调用 `getDataScopeWhere`（basicScope/projectScope/contractScope 三组）

7. **`utils/permission.ts`** — 删除 `getDataFilter`、`getUserDataScope` 两个死函数

8. **6个简单模块** — 参数适配：organizations/quotations/invoices/expenses/businessTrips/dailyReports

**验证结果：**
- TypeScript 编译通过（`tsc --noEmit` 零错误）
- 无残留旧式 `applyDataScope('...')` 调用
- 无残留 `getProjectScopeWhere`/`getDataFilter`/`getUserDataScope` 引用

**二次检查补漏（23:57）：**
- dailyReports.ts 补 6 个路由：`/stats/overview`、`/export/csv`、`/stats/submission-rate`、`/stats/quality`、`/stats/hours-analysis`、`/export/excel`（原来全部无数据权限过滤）
- organizations.ts 补 2 个路由：`/tree`、`/:id`（原来详情和树形结构无数据权限过滤）
- 编译再次验证通过

**已知遗留（原有问题，本次未修改）：**
- 子资源路由文件（contractReceipts/contractShipments/procurementPayments/projectCosts/projectNotes）的列表/详情路由无数据权限过滤
- 各模块的文件下载/预览路由（`/files/:fileId/download` 等）无数据权限过滤，仅靠 checkPermission 控制
- organizations 的 `/simple`、`/contacts/simple` 下拉选择路由无数据权限过滤（可能是有意的）

**决策记录（00:04）：**
- 子资源路由**不补数据权限**——父资源已有 applyDataScope 过滤，子资源在父资源详情页内加载，内部 CRM 风险可控
- 任务模块（tasks.ts）**已有独立的 assigner/assignee 权限检查**，不需要 applyDataScope：
  - 列表：按 assignees/assignerId 过滤，只看自己的任务
  - 详情：检查 isAssigner || isAssignee，否则 403
  - 记录：同样有 assigner/assignee 检查
  - 遗留：任务文件相关路由（GET /:id/files、GET /files/:fileId/download 等）无权限检查，待用户决定是否修复

**任务文件权限修复（00:12）：**
- 新增 `checkTaskAccess(taskId, userId)` 辅助函数，复用 assigner/assignee 检查逻辑
- 修复 6 个路由：
  - `GET /:id/files`（文件列表）— 加 checkTaskAccess(id)
  - `DELETE /:id/files/:fileId`（删除文件）— 加 checkTaskAccess(id)
  - `GET /files/:fileId/download`（下载任务文件）— 从 file.taskId 反查
  - `GET /files/:fileId/preview`（预览任务文件）— 从 file.taskId 反查
  - `GET /records/files/:fileId/download`（下载记录附件）— 通过 include record.taskId 反查
  - `GET /records/files/:fileId/preview`（预览记录附件）— 通过 include record.taskId 反查
- TypeScript 编译通过
