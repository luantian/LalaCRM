# 2026-08-06 工作记录

## 23:46 审批权限集中化管理

根据用户要求，审批权限只保留给管理员账号。

**修改内容：**
- 项目经理角色移除：project:contract:approve、project:procurement:approve
- 测试工程师角色移除：project:contract:approve、project:procurement:approve、finance:expense:approve

**最终权限配置：**
- 管理员(62个权限)：拥有所有审批权限
- 项目经理(21个权限)：无审批权限
- 普通用户(12个权限)：无审批权限
- 测试工程师(25个权限)：无审批权限

---

## 22:50 全面权限测试完成

### 测试范围
- 测试了5个用户：admin、luantian、sales_test、viewer_test、test_user_123
- 测试了18个关键API端点
- 总共90个权限测试用例

### 测试结果
- **通过率**: 100% (90/90)
- 所有API权限检查正常工作
- 不同角色的权限隔离正确实现

### 当前角色权限配置

| 角色 | 数据范围 | 权限数 | 关键权限 |
|------|---------|--------|----------|
| 管理员(admin) | ALL | 63 | 全部功能，包括系统管理、发票管理、报价审批 |
| 普通用户(USER) | ALL | 36 | 客户、项目、售前、报价、费用、日报、出差、考勤，**无**系统管理和发票 |
| 测试工程师(ROLE_MSG896K1) | SELF | 29 | 客户、项目、费用报销 + 日报/出差/考勤（仅列表） |

### 关键权限差异
1. **系统管理**：仅管理员可访问（用户、角色、菜单、部门、字典、日志）
2. **发票管理**：仅管理员可访问
3. **报价单**：管理员可编辑+审批，普通用户仅查看
4. **日报/出差/考勤**：
   - 管理员/普通用户：查看+创建+审批
   - 测试工程师：仅查看列表

### 用户角色分配
- test_admin → 管理员
- test_user1, test_user2, viewer_test, sales_test → 普通用户
- luantian → 无角色（旧roleId=2，但userRoles表为空）

### 修复的问题
- 修复了字典列表接口缺少权限检查的问题
- 修复了发票列表使用错误权限标识的问题

### 注意事项
- 部分用户的旧roleId字段与新的userRoles表不一致
- 建议后续统一使用userRoles表进行角色管理

---

## 权限系统漏洞修复与测试

### 完成的修复
1. **无效Token返回403→401** - 修复 `backend/src/middleware/auth.ts`
2. **角色菜单接口缺少权限检查** - 修复 `backend/src/routes/roleMenus.ts`，为3个路由添加权限检查
3. **删除角色时未清理RoleMenu导致500** - 修复 `backend/src/routes/roles.ts`，删除角色前先清理关联记录
4. **报销创建缺少字段验证导致500** - 修复 `backend/src/routes/expenses.ts`，添加expenseDate必填验证

### 测试结果
- **基础权限测试**: 69/69 通过 (100%)
- **深度权限测试**: 57/69 通过 (82.6%)
- 12个失败用例主要是测试脚本参数问题，非真实Bug

### 新增功能：TEAM数据权限（进行中）
- 在 `DataScope` 枚举中添加了 `TEAM` 类型
- 数据库迁移已完成（使用 `ALTER TYPE "DataScope" ADD VALUE 'TEAM'`）
- 扩展了 `getDataScopeWhere` 函数支持 `teamMemberField` 参数
- 项目列表/详情/统计接口使用新的团队权限逻辑
- 前端角色管理页面显示TEAM数据范围

### 服务状态
- 后端: http://localhost:5000 ✓
- 前端: http://localhost:3000 ✓

### 待完成
- TEAM数据权限功能的前端配置界面
- 测试TEAM数据权限的实际效果

---

## 15:30 TEAM数据权限功能测试完成

### 实现内容
1. **数据模型**：DataScope枚举添加TEAM值
2. **权限中间件**：扩展`getDataScopeWhere`支持团队字段参数
3. **项目管理**：实现团队数据权限过滤逻辑
4. **权限工具**：修复`getUserDataScope`回退到User.roleId查询

### 测试验证
用户3（栾天，TEAM数据范围）项目列表接口返回：
- 自己创建的项目（4个）：项目2,3,4,5
- 作为团队成员的项目（1个）：项目1

**总计5个项目，TEAM权限正常工作** ✅

### 技术细节
- 查询条件：`deletedAt: null AND (ownerId = userId OR teamMembers.some.userId = userId)`
- 软删除项目正确过滤
- 团队成员关系正确关联

---

## 19:30 权限清理与合同采购权限收紧

### 清理废弃功能权限代码
修复了`RoleManagement.tsx`中删除`permissionDefs`常量后残留引用的问题：
- 清理了`selectedPermissions`状态和相关逻辑
- 删除了权限对照表矩阵代码
- 删除了编辑弹窗中的功能权限Checkbox区域
- 移除了无用import

### 合同和采购权限收紧
用户要求：通过项目成员加进来的用户不能看到合同管理和采购管理。
- 合同列表/详情/文件下载：只允许管理员或合同负责人访问
- 采购列表/详情：只允许管理员或采购负责人访问
- 项目团队成员无法查看这两个模块

---

## 23:00 权限配置精简调整

### 问题
用户要求审批权限只保留给管理员，普通角色不需要审批权限。

### 调整内容

**1. 创建项目经理角色（ID:40，roleKey:PROJECT_MANAGER）**
- 分配给 luantian 用户
- 权限：仪表盘 + 客户管理 + 售前管理 + 项目管理（合同+采购，无审批）
- 共23个权限，移除费用报销和日常办公权限

**2. 精简普通用户角色（ID:6，roleKey:USER）**
- 权限：仪表盘 + 客户管理 + 售前管理 + 报价单（仅查看）
- 共12个权限，移除项目管理、采购管理、费用报销、日常办公权限

**3. 审批权限集中管理**
- 所有审批权限（合同、采购、费用报销、日报、出差）仅保留给管理员
- 项目经理和普通用户无法执行审批操作

### 最终权限矩阵

| 角色 | 权限数 | 核心权限 |
|------|--------|----------|
| **管理员** | 62 | 全部功能，包括系统管理、发票管理、所有审批权限 |
| **项目经理** | 23 | 客户、售前、项目管理、合同、采购（无审批） |
| **普通用户** | 12 | 客户、售前、报价单查看 |
| **测试工程师** | 28 | 客户、项目、费用报销 + 日报/出差/考勤（仅列表） |
